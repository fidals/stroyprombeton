Deploy tasks for SE version 1:
- Run transfer command
- Run texts command
- Run gulp build
- Check all phones on site


Standard deploy instructions:

docker login -u duker
docker pull fidals/stb
cd ~/stroyprombeton/docker/
docker-compose -f docker-compose.yml -f production.yml up -d
docker exec -it se python3 /opt/stroyprombeton/manage.py migrate
docker exec -it se python3 /opt/stroyprombeton/manage.py catalog
